// Runtime evidence must come from saved post-match reports, video tags/tracks
// or staff-entered notes. Keep this empty so the coach never treats demo
// observations as current evidence.
export const MATCH_OBSERVATIONS = [];
